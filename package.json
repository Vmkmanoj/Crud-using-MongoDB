{
  "name": "crud-operation",
  "version": "1.0.0",
  "scripts": {
    "start": "concurrently \"npm run dev --prefix crud\" \"npm run start --prefix backend\"",
    "install": "npm install --prefix frontend && npm install --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^6.0.0"
  }
}
